# Copy to .env and set values. Used by run_email_agent.py (Phase 3).
# Do NOT commit .env (it contains your app password).

# IMAP (read inbox) — use your Gmail address and a Gmail App Password
UARB_IMAP_HOST=imap.gmail.com
UARB_IMAP_PORT=993
UARB_IMAP_USER=s012820041010@gmail.com
UARB_IMAP_PASSWORD=kpqf vayc ulwc zkvj

UARB_IMAP_SSL=true

# SMTP (send reply with ZIP) — same Gmail + same App Password
UARB_SMTP_HOST=smtp.gmail.com
UARB_SMTP_PORT=587
UARB_SMTP_USER=s012820041010@gmail.com
UARB_SMTP_PASSWORD=kpqf vayc ulwc zkvj
UARB_SMTP_TLS=true

# Optional: poll interval in seconds (default 30)
# UARB_POLL_INTERVAL_SEC=30

# Local prompt flow (run_local_prompt.py): send ZIP to this address
# UARB_SEND_ZIP_TO=s012820041010@gmail.com
